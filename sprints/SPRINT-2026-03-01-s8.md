# Sprint 8 — Forge Voicing Generator + Content Depth
*2026-03-01*

## Goal
Make diatonic suggestion chips actually useful by generating first-position voicings. Fix the 7th degree color clash with root. Add 15 mood profiles to reach 105.

## Decisions
- Voicing generator is algorithmic, not hardcoded: for each string, find lowest fret 0–4 that plays a chord tone. Tuning-aware — works for all 8 tunings. Produces valid (if not always conventional) voicings; e.g. D major includes all 6 strings, which is technically correct.
- 7th degree color changed to steel blue (#6a8a9a) — cool and muted, reads clearly different from the amber root on dark background.
- Chip click handler updated in `updateForge()` — one line change, voicing passed directly to progression entry.

## Tasks
- [x] Change 7th degree color in `DEGREE_COLORS` (#b88a3a → #6a8a9a)
- [x] Add `generateFirstPositionVoicing(chordName, tuningId)` to `script.js`
- [x] Update suggestion chip click handler to use `generateFirstPositionVoicing()`
- [x] Add 15 mood profiles to `data/mood-map.js` (90 → 105)
- [x] Update ROADMAP.md — v8 shipped block, refresh Next section
- [x] Archive sprint
